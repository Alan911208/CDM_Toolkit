============================================================
TEST RESULTS: runDateDiff
============================================================

Test Environment:
  - OLD dataset (dm_old): 3 rows, rundate = 01JAN2020
    Row 1: 001-0001, S001, TRT
    Row 2: 001-0002, S001, PBO
    Row 3: 001-0003, S002, TRT

  - NEW dataset (dm_new): 4 rows (NO pre-existing rundate column)
    Row 1: 001-0001, S001, TRT  (same as old = UNCHANGED)
    Row 2: 001-0002, S001, TRT  (ARMCD changed PBO->TRT = CHANGED)
    Row 3: 001-0003, S002, TRT  (same as old = UNCHANGED)
    Row 4: 001-0004, S003, PBO  (new subject = NEW)

  - Key variable: usubjid (unique per subject)
  - Excluded variables: rundate (the date column itself is not compared)

============================================================
EXPECTED OUTPUT
============================================================

[Test 1] Output dataset dm_result has 4 observations (all from new).

[Test 2] Unchanged rows (001-0001, 001-0003):
  - usubjid=001-0001: siteid=S001, armcd=TRT
    rundate = 01JAN2020  (KEPT from old — row is identical)
  - usubjid=001-0003: siteid=S002, armcd=TRT
    rundate = 01JAN2020  (KEPT from old — row is identical)

[Test 3] Changed row (001-0002):
  - usubjid=001-0002: siteid=S001, armcd=TRT (was PBO)
    rundate = TODAY'S DATE (formatted from &sysdate9.)
    NOT 01JAN2020 — because ARMCD differs from the old version

[Test 4] New row (001-0004):
  - usubjid=001-0004: siteid=S003, armcd=PBO
    rundate = TODAY'S DATE (formatted from &sysdate9.)
    No matching row in old — treated as new data

[Test 5] PROC FREQ of dm_result.rundate:
  - 01JAN2020: count = 2 (unchanged rows)
  - Today's date: count = 2 (changed + new rows)

============================================================
VERIFICATION CRITERIA
============================================================

PASS: The output dataset contains all rows from the new dataset (4 rows).
PASS: Rows with identical data across all compared columns retain the
      original rundate from the old dataset.
PASS: Rows with any changed value get assigned &sysdate9. as rundate.
PASS: Completely new rows (no match in old by keyvar) get &sysdate9.
PASS: The temporary underscore-prefixed columns (_siteid, _armcd) and
      _rundate are dropped from the output.
PASS: The key variable (usubjid) is present and unmodified.

============================================================
NOTES
============================================================

- The comparison excludes variables listed in 'exvar'.  rundate MUST be
  in exvar to prevent the date column itself from being compared as data.
- All non-key, non-exclude columns from new are compared against their
  underscore-prefixed counterparts from old.
- A row that exists in old but NOT in new is silently dropped (output
  comes from the new dataset only).
